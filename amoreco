<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Para o Amor da Minha Vida ❤️</title>

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    min-height: 100vh;
    padding: 60px 0;
    overflow-x: hidden;
}

.container {
    background: white;
    max-width: 1200px;
    margin: auto;
    width: 95%;
    padding: 60px 50px;
    border-radius: 30px;
    text-align: center;
    box-shadow: 0 20px 50px rgba(0,0,0,0.25);
}

h1 {
    color: #ff4b6e;
    font-size: clamp(2.5rem, 4vw, 3.4rem);
    margin-bottom: 25px;
}

p {
    color: #555;
    font-size: 1.25rem;
    line-height: 1.9;
    margin-bottom: 30px;
}

.heart {
    font-size: 2.8rem;
    animation: pulse 1.5s infinite;
    margin-bottom: 35px;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.3); }
    100% { transform: scale(1); }
}

/* GALERIA */
.gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 35px;
    margin-top: 50px;
}

/* CARD */
.photo-card {
    background: #fff0f3;
    border-radius: 25px;
    padding: 18px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    cursor: pointer;
    opacity: 0;
    transform: translateY(50px);
    transition: 0.8s;
}

.photo-card.show {
    opacity: 1;
    transform: translateY(0);
}

/* IMAGEM */
.photo-card img {
    width: 100%;
    height: 300px;
    object-fit: cover;
    border-radius: 20px;
    filter: blur(12px);
    transition: 0.8s;
}

/* TEXTO */
.caption {
    margin-top: 15px;
    color: #ff4b6e;
    font-size: 1.1rem;
    opacity: 0;
    transition: 0.6s;
}

/* REVELADO */
.photo-card.revealed img {
    filter: blur(0);
}

.photo-card.revealed .caption {
    opacity: 1;
}

footer {
    margin-top: 60px;
    font-size: 1rem;
    color: #888;
}

/* Corações */
.floating-heart {
    position: fixed;
    bottom: -40px;
    animation: floatUp linear forwards;
    pointer-events: none;
}

@keyframes floatUp {
    to {
        transform: translateY(-120vh);
        opacity: 0;
    }
}

/* MOBILE */
@media(max-width:600px){
    .container{
        padding: 40px 20px;
    }
    .photo-card img{
        height: 230px;
    }
}
</style>
</head>

<body>

<div class="container">
    <h1>Para Você, Meu Amor ❤️</h1>

    <p>
        Cada momento ao seu lado é um presente.
        Desça a página e revele nossas lembranças,
        uma por uma, assim como nosso amor cresce todos os dias.
    </p>

    <div class="heart">💖 💕 💘</div>

    <div class="gallery">

        <div class="photo-card">
            <img src="img/foto1.jpeg">
            <div class="caption">Você me faz feliz cada dia mais 💕</div>
        </div>

        <div class="photo-card">
            <img src="img/foto2.jpeg">
            <div class="caption">Meu lugar favorito sempre será você ❤️</div>
        </div>

        <div class="photo-card">
            <img src="img/foto3.jpeg">
            <div class="caption">Momentos simples que viraram eternos ✨</div>
        </div>

        <div class="photo-card">
            <img src="img/foto4.jpeg">
            <div class="caption">Com você, tudo faz sentido 💖</div>
        </div>

    </div>

    <footer>Feito com ❤️ por Guilherme</footer>
</div>

<script>
/* Scroll reveal */
const cards = document.querySelectorAll('.photo-card');

const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if(entry.isIntersecting){
            entry.target.classList.add('show');
            observer.unobserve(entry.target);
        }
    });
},{ threshold: 0.2 });

cards.forEach(card => observer.observe(card));

/* Clique para revelar */
cards.forEach(card => {
    card.addEventListener('click', () => {
        card.classList.toggle('revealed');
    });
});

/* Corações */
setInterval(() => {
    const heart = document.createElement('div');
    heart.className = 'floating-heart';
    heart.innerHTML = '❤️';
    heart.style.left = Math.random() * 100 + 'vw';
    heart.style.fontSize = Math.random() * 25 + 15 + 'px';
    heart.style.animationDuration = Math.random() * 3 + 4 + 's';
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}, 600);
</script>

</body>
</html>
